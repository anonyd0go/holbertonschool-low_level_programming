#include "main.h"

/**
 * add - adds two integers
 * @i: one of the integers to sum
 * @n: other integer tu sum
 * Return: Result of the sum of the digits
 */
int add(int i, int n)
{
	return (i + n);
}
